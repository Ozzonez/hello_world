#include <iostream>
#include <vector>
#include "kierowcy.h"
#include "samochody.h"
using namespace std;

int main()
{
    //Engine e1(dasd);
    //Engine e2(dasd);
    //Engine e3(dasd);

    //Driver t[11];

   /* Driver D;
    vector<Driver> Dr;               */

    /*for(int i=1; i<=11; i++)
    {
        if(t[i].load(i)==0)
            break;
    }
    t[2].show();*/

/*


    int i=0;
    while(1)
    {
        Dr.push_back(D);
        if(Dr[i].load(i+1)==0)
        {
            Dr.pop_back();
            break;
        }

        i++;

    }
    i--;
    cout<<i;
    Dr[i].show();

    cout<<

*/

/*
    Car c1;
    Car c2("dupa");

    c1=c2;

    cout<<c1.performance();
    cout<<c2.performance();
    */













    return 0;
}
